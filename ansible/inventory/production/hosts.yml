---
all:
  children:
    ipfs_nodes:
      hosts:
        ipfs01.example.com:
        ipfs02.example.com:
      vars:
        ansible_user: ubuntu
        ansible_become: yes
        ansible_python_interpreter: /usr/bin/python3
